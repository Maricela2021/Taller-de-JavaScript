// Ejemplo de valor para la nota, escenario 1: Aprobado
let nota1 = 4; 

if (nota1 >= 3) {
    console.log("Nota:", nota1, "Aprobado");
} else {
    console.log("Nota:", nota1, "Reprobado");
}

// solo cambiamos el valor, escenario 2: Reprobado
let nota2 = 2; 

if (nota2 >= 3) {
    console.log("Nota:", nota2, "Aprobado");
} else {
    console.log("Nota:", nota2, "Reprobado");
}